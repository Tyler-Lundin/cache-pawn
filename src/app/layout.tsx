import type { Metadata } from "next";
import { Roboto, Roboto_Mono } from "next/font/google";
import "./globals.css";

const roboto = Roboto({
  variable: "--font-roboto",
  subsets: ["latin"],
  weight: ["200", "300", "400", "500", "700", "900"],
});

const robotoMono = Roboto_Mono({
  variable: "--font-roboto-mono",
  subsets: ["latin"],
  weight: ["200", "300", "400", "500", "700",],
});

export const metadata: Metadata = {
  title: "Fast Cache Pawn",
  description: "Fast Cache Pawn is a pawn shop in Logan, UT that offers a wide range of services including cash loans, jewelry, musical instruments, electronics, and more. Our experienced staff is here to help you find exactly what you need.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${roboto.variable} ${robotoMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
